# This is used as a framework for the machine learning model pipeline
def load():
  pass
def preprocess():
  pass
def train():
  pass
def validate():
  pass
def aggregate(): # Used for aggregating the final statistics
  pass

if __name__ == "main":
  # load()
  # TODO: Have the framework automatically generate a log and store it in the `outputs` folder,
  # uniquely named by the run datetime, and have a copy of its hyperparameters via `param.py`.
